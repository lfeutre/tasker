[{tasker,
  [{tasks, [[{name, "Example Remote Task (execute a command over SSH)"},
             {cmd, "ssh"},
             {args, ["musicserver@music.local", "\"ls -al ./files\""]},
             {interval, 12}]]
  }]
}].